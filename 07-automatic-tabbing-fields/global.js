window.onload = function() {

  //first search document for first id
  // write code that says
    // when the length of that entry is == the maxlength
    // then move the cursor to the next field

};
